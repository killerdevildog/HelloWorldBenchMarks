# Csh (run): csh hello.csh
# Csh (executable): chmod +x hello.csh && ./hello.csh
# Csh (source): csh -c "source hello.csh"
# Csh (command): csh -c "echo 'Hello World!'"
# Csh (script): csh < hello.csh
# Csh (interactive): csh (then source hello.csh)
# Tcsh (run): tcsh hello.csh
# Csh (fast): csh -f hello.csh

#!/bin/csh
echo "Hello World!"
