// Swift (run): swift hello.swift
// Swift (binary): swiftc hello.swift -o hello && ./hello
// Swift Package Manager: swift run
// Swift (optimized): swiftc -O hello.swift -o hello && ./hello
// Xcode: xcodebuild -project Hello.xcodeproj -scheme Hello build && ./build/Release/Hello
// Swift REPL: swift (then paste code)
// Swift Playgrounds: Open in Swift Playgrounds
// Swift for TensorFlow: swiftc hello.swift -ltensorflow && ./hello

print("Hello World!")
