//F# Compiler for F# 4.1
// F# Interactive: dotnet fsi hello.fsx
// F# Compiler: fsharpc hello.fs -o hello.exe && hello.exe
// dotnet CLI: dotnet run (with .fsproj)
// dotnet build: dotnet build && dotnet run
// Visual Studio: F5 (run) or Ctrl+Shift+B (build)
// MSBuild: msbuild hello.fsproj && ./bin/Debug/hello.exe
// Mono F#: fsharpc hello.fs && mono hello.exe
// Ionide: F5 in VS Code with Ionide extension
open System
printfn "Hello, World!"
