# Zsh (run): zsh hello.zsh
# Zsh (executable): chmod +x hello.zsh && ./hello.zsh
# Zsh (source): zsh -c "source hello.zsh"
# Zsh (command): zsh -c "echo 'Hello World!'"
# Zsh (script): zsh < hello.zsh
# Zsh (interactive): zsh (then source hello.zsh)
# Zsh (no-rcs): zsh --no-rcs hello.zsh
# Zsh (login): zsh -l hello.zsh

#!/usr/bin/env zsh
echo "Hello World!"
