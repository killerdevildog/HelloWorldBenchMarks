# Fish (run): fish hello.fish
# Fish (executable): chmod +x hello.fish && ./hello.fish
# Fish (source): fish -c "source hello.fish"
# Fish (command): fish -c "echo 'Hello World!'"
# Fish (script): fish < hello.fish
# Fish (interactive): fish (then source hello.fish)
# Fish (no-config): fish --no-config hello.fish
# Fish (private): fish --private hello.fish

#!/usr/bin/env fish
echo "Hello World!"
